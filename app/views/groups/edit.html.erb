<div class="edit">
  <h2 class="title">グループ情報</h2>
  <%= form_with model: @group, local: true, class:"edit-form" do |f| %>
    <div class="field">
      <%= f.label :name, "グループ名", class: "label" %>
      <%= f.text_field :name, value: @group.name, size: 35, class: "form" %>
    </div>
    <div class="field">
      <%= f.label :description, "概要説明", class: "label" %>
      <%= f.text_area :description, value: @group.description, class: "form" %>
    </div>
    <div class="field">
      <%= f.label :public, "公開設定", class: "label" %>
      <div class="radio">
        <label><%= f.radio_button :public, true %> 参加リクエスト可能</label>
      </div>
      <div class="radio">
        <label><%= f.radio_button :public, false %> メンバーによる招待制</label>
      </div>
    </div>
    <div class="actions">
      <%= f.submit "変更を保存", class:"form-button" %>
    </div>
  <% end %>
  <h2 class="title">グループを退会</h2>
  <div class="unsubscribe">
    <div class="actions">
      <p>「<%= @group.name %>」を退会します。<br>この操作を取り消したい場合は、再度参加リクエストをするか、グループのメンバーに招待を依頼してください。<br><span>※自分以外にメンバーが一人もいない場合は、グループごと削除されます。</span></p>
      <%= link_to '退会する', user_group_relation_path(@group.relation(current_user)), method: :delete, class: "form-button withdraw", data: { confirm: "【確認】#{@group.name}を退会します" } %>
    </div>
  </div>
  <h2 class="title">グループの削除</h2>
  <div class="unsubscribe">
    <div class="actions">
      <%= link_to 'グループを削除する', delete_group_path(@group), class: "form-button" %>
    </div>
  </div>
</div>