<!DOCTYPE html>
<html>
  <head>
    <title>Mates</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Sawarabi+Gothic" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=M+PLUS+Rounded+1c" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application' %>
    <%= javascript_pack_tag 'users/show', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if notice || alert %>
      <div class="notifications"><%= notice %><%= alert %></div>
    <% end %>
    <header><%= link_to 'Mates', root_path %></header>
    <% if current_user %>
      <div class="contents">
        <%= render partial: 'layouts/sidebar' %>
        <div class="main-contents"><%= yield %></div>
      </div>
    <% else %>
      <div class="contents">
        <%= yield %>
      </div>
    <% end %>
    <footer><p class="copyright">&copy; 2022 keita inui </p></footer>
  </body>
</html>
