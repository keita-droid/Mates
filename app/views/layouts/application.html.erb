<!DOCTYPE html>
<html lang="ja">
  <head>
    <title><%= yield :title %>Mates</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@100;400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=M+PLUS+Rounded+1c" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <% if notice %>
      <div class="notifications notice"><p><%= notice %></p></div>
    <% end %>
    <% if alert %>
      <div class="notifications alert"><p><%= alert %></p></div>
    <% end %>
    <!-- header -->
    <header class="header">
      <h1 class="logo">
        <%= link_to 'Mates', root_path %>
      </h1>
      <nav class="g-nav">
        <% if current_user%>
        <ul>
          <li><%= link_to groups_path do %><%= fa_icon "users" %>グループ<% end %></li>
          <li><%= link_to  user_path(current_user) do %><%= fa_icon "user-circle-o" %>マイページ<% end %></li>
          <li><%= link_to destroy_user_session_path, method: :delete do %><%= fa_icon "sign-out" %>ログアウト<% end %></li>
        </ul>
        <% end%>
      </nav>
    </header>
    <!--  header End -->
    <!-- main -->
    <% if current_user %>
      <div class="main">
        <%= yield %>
      </div>
    <% else %>
      <div class="main">
        <%= yield %>
      </div>
    <% end %>
    <!-- main  End -->
    <!-- footer -->
    <footer class="footer">
      <div class="footer-contents">
        <ul>
          <li><%= link_to 'このアプリについて', helps_policies_path, class: "helps-button" %></li>
          <li><%= link_to 'よくある質問', helps_policies_path, class: "helps-button" %></li>
          <li><%= link_to 'プライバシーポリシー', helps_policies_path, class: "helps-button" %></li>
        </ul>
      </div>
      <p class="copyright">&copy; app-mates 2022</p>
    </footer>
    <!-- footer  End -->
  </body>
</html>
